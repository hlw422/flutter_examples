import 'package:flutter/material.dart';

class NumberOneWidget extends StatelessWidget {
  const NumberOneWidget({super.key});
  @override
  Widget build(BuildContext context) {
    return Column(
      mainAxisAlignment: MainAxisAlignment.start,
      children: [
        Row(
          
        ),
      ],
    );
  }
}
